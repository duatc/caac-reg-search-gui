# CAAC-RegSearch 图形界面设计规范

## 1. 概念与愿景

这是一款面向民航一线管制员和安全管理人员的**本地民航法规智能检索工具**。
界面以专业沉稳的航空蓝色调为核心，交互简洁高效，无需联网，数据全部本地化。
目标是让法规查询像翻阅手册一样直观，但比任何手册都更快、更精准。

## 2. 设计语言

### 色彩体系
| 用途 | 色值 | 说明 |
|------|------|------|
| 主色/导航栏 | #0d2137 | 深海蓝，权威稳重 |
| 强调色 | #1a5fa8 | 航空蓝 |
| 强调色(亮) | #3a8ae8 | 悬停/渐变末端 |
| 背景色 | #f0f4f8 | 浅灰蓝 |
| 卡片背景 | #ffffff | 纯白 |
| 正文主色 | #1a1a2e | 近黑 |
| 正文辅助 | #5a6a7a | 蓝灰 |
| 边框色 | #d0dae8 | 淡蓝灰 |
| 高亮背景 | #fff3cd | 关键词高亮 |
| 高亮文字 | #c0392b | 红色高亮 |

### 字体
- 主字体：`微软雅黑` / `思源黑体` / `Noto Sans SC`
- 正文字号：14px，行高 1.9
- 标题字号：17px，bold
- 小标签：11~12px

### 空间系统
- 侧边栏宽度：260px
- 主内容最小宽度：940px
- 卡片间距：8px
- 内边距：16~20px
- 搜索框高度：48px（主），40px（侧边栏）

### 动效
- 按钮悬停：0.2s 渐变背景色
- 卡片悬停：0.2s 边框变蓝 + 轻微背景变亮
- 搜索加载：按钮禁用 + 状态栏文字提示
- 索引状态：绿色"● 已就绪" / 红色"⚠ 异常"

## 3. 布局结构

```
┌─────────────────────────────────────────────────────────┐
│  顶部工具栏：标题 + 条目数量 + 索引状态                     │ 46px
├────────────┬────────────────────────────────────────────┤
│            │  主搜索框：placeholder + 搜索按钮 + 清空按钮   │ 72px
│  260px     ├──────────────────────┬─────────────────────┤
│  侧边栏    │                      │                     │
│            │   搜索结果列表        │   条款详情面板       │
│  · Logo    │   (卡片式)           │   (选中后显示)       │
│  · 搜索框  │                     │                     │
│  · 分类导航│                     │   · 标题/来源/得分   │
│  · 版本信息│                     │   · 高亮正文        │
│            │                     │   · 复制全文按钮    │
└────────────┴──────────────────────┴─────────────────────┘
│  状态栏                                                    │ 28px
└─────────────────────────────────────────────────────────┘
```

## 4. 功能模块

### 4.1 搜索
- **双入口**：顶部主搜索框 + 侧边栏搜索框，同步触发
- **关键词高亮**：结果摘要中命中的分词片段用黄色背景+红字标出
- **相关度评分**：右上角用颜色区分（红=高，绿=低）
- **防抖**：支持回车触发，支持清空按钮一键重置

### 4.2 分类导航
- 7个分类：全部 / 管制运行 / 情报通信 / 航空器运行 / 维修管理 / 安全管理 / 通航经营
- 点击后自动过滤当前结果，保留关键词检索上下文
- 当前选中项蓝色高亮

### 4.3 结果列表
- 每张卡片：排名 + 法规名称 + 相关度得分 + 来源文件 + 摘要
- 悬停卡片：边框变蓝，轻微背景变亮
- 点击卡片：右侧详情面板展示完整条款内容
- 空结果：友好提示，换词建议

### 4.4 条款详情
- 渐变色头部：标题 + 来源文件
- 全文展示，支持复制
- 关键词全文高亮（不只是摘要）
- 滚动阅读，保留浏览位置

### 4.5 索引状态
- 启动时自动检测 docs.json 是否存在
- 显示文件大小（KB）
- 预加载索引加速首次搜索

## 5. 组件清单

| 组件 | 状态 |
|------|------|
| 搜索框 | default / focus / disabled(检索中) |
| 搜索按钮 | default / hover / disabled |
| 分类按钮 | default / hover / selected(active) |
| 结果卡片 | default / hover / selected |
| 详情面板 | empty(提示) / loaded(内容) |
| 索引状态 | 未加载(红) / 加载中(黄) / 已就绪(绿) |
| 状态栏 | 就绪 / 检索中 / 检索完成 |

## 6. 技术实现

- **框架**：PyQt5（支持 Windows / macOS / Linux）
- **打包**：PyInstaller（build.bat 已有配置）
- **依赖**：PyQt5, jieba, rank-bm25, uvicorn（GUI模式不需要）
- **入口**：`main_window.py` 中的 `main()` 函数
- **打包入口**：`CAAC-RegSearch.spec`（需更新添加 main_window.py）
